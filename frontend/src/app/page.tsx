'use client';
import {useEffect,useState} from 'react';
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8080';
export default function Home(){const [wallet,setWallet]=useState<any>();const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{fetch(API+'/api/v1/wallet',{credentials:'include'}).then(r=>{if(!r.ok)throw new Error('Please sign in');return r.json()}).then(setWallet).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <main className="mx-auto max-w-6xl px-6 py-12"><div className="mb-10"><p className="gold text-sm font-semibold uppercase tracking-widest">Financial control, simplified</p><h1 className="mt-2 text-4xl font-bold">Your money, clearly.</h1><p className="mt-3 max-w-2xl text-slate-400">A secure wallet and payment dashboard backed by a double-entry financial ledger.</p></div>{loading?<div className="card">Loading wallet…</div>:error?<div className="card text-slate-300">{error}. <a className="gold underline" href="/login">Sign in</a></div>:<div className="card max-w-md"><p className="text-sm text-slate-400">Available balance</p><p className="mt-2 text-4xl font-bold">{wallet.currency} {(wallet.availableBalance/100).toLocaleString(undefined,{minimumFractionDigits:2})}</p><p className="mt-3 text-xs text-slate-500">Authoritative balance from Nagode ledger</p></div>}</main>}
