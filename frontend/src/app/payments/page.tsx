'use client';
import {useEffect,useState} from 'react';
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8080';
export default function Payments(){const [items,setItems]=useState<any[]>([]);const [error,setError]=useState('');useEffect(()=>{fetch(API+'/api/v1/payments',{credentials:'include'}).then(async r=>{if(!r.ok)throw new Error('Unable to load payments');return r.json()}).then(setItems).catch(e=>setError(e.message))},[]);return <main className="mx-auto max-w-6xl px-6 py-12"><h1 className="text-3xl font-bold">Payments</h1>{error&&<p className="mt-5 text-red-400">{error}</p>}<div className="mt-6 space-y-3">{items.length===0&&!error?<div className="card text-slate-400">No payments yet.</div>:items.map(p=><div className="card flex items-center justify-between" key={p.id}><div><p className="font-semibold">{p.currency} {(p.amount/100).toLocaleString()}</p><p className="text-xs text-slate-500">{p.id}</p></div><span className="gold text-sm">{p.status}</span></div>)}</div></main>}
