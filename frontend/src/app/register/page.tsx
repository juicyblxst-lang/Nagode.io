'use client';
import {FormEvent,useState} from 'react';
export default function Register(){const [form,setForm]=useState({email:'',password:'',displayName:''});const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();const r=await fetch((process.env.NEXT_PUBLIC_API_URL||'http://localhost:8080')+'/api/v1/auth/register',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(!r.ok){setError('Registration failed');return}location.href='/login'}return <main className="mx-auto max-w-md px-6 py-20"><div className="card"><h1 className="text-2xl font-bold">Create your Nagode account</h1><form onSubmit={submit} className="mt-6 space-y-4">{[['displayName','Name','text'],['email','Email','email'],['password','Password','password']].map(([k,p,t])=><input key={k} required type={t} minLength={k==='password'?10:1} placeholder={p} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="w-full rounded-lg bg-slate-900 p-3"/>)}<button className="w-full rounded-lg bg-amber-400 p-3 font-semibold text-slate-950">Create account</button>{error&&<p className="text-sm text-red-400">{error}</p>}</form></div></main>}
